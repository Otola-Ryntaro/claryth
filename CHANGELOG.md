# Changelog

## 0.2.0 - 2026-06-13

製品化・セキュリティレビューで見つかった問題を修正し、臨床利用不可の評価版を安全に検証・配布できる状態へ更新した。

### 安全性とデータ承認

- 医学レビュー未完了、署名不正、期限切れ、DB改変時は判定をfail-closedで停止する。
- トップ20候補へ行単位の内容ハッシュ、レビュー状態、レビュー者、日時、承認IDを追加した。
- 全候補を網羅するレビューCSVの取込と、`reviewed`行だけを実行DBへ昇格する処理を追加した。
- 薬剤名マスター対応、手動シード差分、全対象・全成分のゴールデン結果を生成する。
- 承認済みゴールデン結果がDBハッシュと全期待値へ一致しない製品bundleを拒否する。

### セキュリティ

- Chrome拡張とローカルAPIへ共有トークン、固定アプリID、プロトコル版、起動nonceによる相互確認を追加した。
- CORSを固定拡張Originへ限定し、入力長、32 KiBボディ上限、同時実行数、レートを制限した。
- Ed25519署名manifest、DB SHA-256、SQLite完全性、スキーマ版、有効期限を起動時に検証する。
- 製品モードではOpenAPI、Swagger UI、ReDocを公開しない。

### 依存関係と配布

- Python依存を`uv.lock`へハッシュ付きで固定し、Node依存を`npm ci`で再現可能にした。
- Python/npm脆弱性監査、例外期限検証、CycloneDX SBOM生成をCIへ追加した。
- 署名済み固定ZIPと全ファイルmanifestの作成・検証処理を追加した。
- bundleから秘密鍵、APIトークン、候補DB、レビュー成果物、原XML/PDF/CSVを除外した。
- 緊急停止、更新、ロールバック、監査、出荷チェックリスト、実機E2E記録票を整備した。

### UIとアクセシビリティ

- 画面上部へ`判定利用可`、`臨床利用禁止`、`判定利用不可`と理由を常時表示する。
- 重要文字を12px以上とし、キーボードフォーカス、強制カラーモード、狭幅・200%ズームへ対応した。
- 未レビュー・完全性エラー時は判定操作を無効化する。

### 検証

- Pythonテスト: 82件成功
- 拡張UIテスト: 5件成功
- TypeScriptビルド: 成功
- Python/npm依存監査: 既知脆弱性0件
- 評価版bundle: 署名、全53ファイル、公開鍵fingerprintを検証済み

### 未完了の製品出荷ゲート

- 実データに対する医師・薬剤師の承認
- 製品責任者、医学データ責任者、セキュリティ窓口、配布担当者の実名割当
- PMDA由来生成DBの配布範囲に関する法務・権利者確認
- クリーンなWindows 10/11とChromeでの導入・更新・ロールバック・アクセシビリティ実機記録

これらが完了するまで、`0.2.0`は臨床利用禁止の評価版として扱う。

## 0.1.0 - 2026-06-12

- Chromeサイドパネル、ローカルFastAPI、SQLiteによる初期評価版を公開した。
