# 🔴 ベーシックルール（必ず守る）

LLM がコーディングで犯しがちなミスを減らすための基本ルール。**簡単なタスクでは判断を委ねるが、原則として下記4点を常に意識する。**

## 1. コーディング前に考える（Think Before Coding）

**思い込まない。混乱を隠さない。トレードオフを表面化する。**

実装前に：

- 前提を明示的に述べる。不確実なら質問する（特に確信度 80% 未満なら明示）。
- 複数の解釈が成り立つ場合は提示し、黙って選ばない。
- よりシンプルな方法があるなら、そう言う。必要なら押し戻す。
- 何かが不明瞭なら止まる。何が混乱しているかを名指しで質問する。
- 要件があいまい・セキュリティに関わる・UX / API 契約に影響する場合は、必ず相談・エスカレートする。

## 2. シンプルさ優先（Simplicity First / YAGNI）

**問題を解く最小限のコードのみ。投機的な実装はしない。**

- 求められていない機能・将来の拡張は実装しない。
- 単発利用のための抽象化は作らない。
- 求められていない「柔軟性」「設定可能性」は持ち込まない。
- 起こり得ないシナリオへのエラーハンドリングは書かない。
- 新しい依存は本当に必要な場合のみ追加し、不要になったら削除する。
- 200 行で書いたものが 50 行で済むなら書き直す。

自問: 「シニアエンジニアならこれを過剰実装と言うか？」 Yes ならシンプル化する。

## 3. 外科的変更（Surgical Changes）

**触る必要のあるところだけ触る。常に最小限・安全・可逆的に。**

既存コードを編集するとき：

- 隣接するコード・コメント・フォーマットを「改善」しない。
- 壊れていないものをリファクタリングしない。
- 自分なら違うやり方をする場合でも、既存スタイルに合わせる。
- 関係のない dead code に気付いても、削除せず指摘するだけにする。

変更によって孤立物（orphan）が発生したとき：

- **自分の変更によって**未使用になった import・変数・関数は削除する。
- 既存の dead code は依頼されない限り削除しない。

判定基準: 変更されたすべての行は、ユーザーの依頼に直接トレース可能であること。

## 4. ゴール駆動の実行（Goal-Driven Execution）

**成功条件を定義する。検証されるまでループする。正確さを速度より優先する。**

タスクを検証可能なゴールに変換する（TDD 原則）：

- 「バリデーションを追加」→ 「不正入力に対するテストを書き、それを通す」
- 「バグを修正」→ 「再現テストを書き、それを通す」
- 「X をリファクタ」→ 「リファクタ前後でテストが通ることを保証」

実装中はテストの意図を変えず、コード側を修正してテストを通す。各変更で少なくとも 1 つはテストを追加・更新する。

複数ステップのタスクでは、簡潔なプランを示す：

```text
1. [ステップ] → 検証: [チェック]
2. [ステップ] → 検証: [チェック]
3. [ステップ] → 検証: [チェック]
```

表面的な修正ではなく根本原因を意識し、近接するリスクも含めて再発防止を考える。

---

**これらのルールが機能している兆候:** diff における不要な変更が減る／過剰実装による書き直しが減る／実装の後ではなく前に確認質問が出てくる。

---

# Global Standards（ベーシックルールを補う具体規約）

## Code Style & Limits

- 1 ファイルあたりおおよそ 300 行以内を目安とし、モジュールはできるだけ単一責務に保つ。
- 各ファイルの先頭に簡潔なヘッダーコメント（where / what / why）を書く。
- 読み手にとって分かりにくい箇所にはコメントで意図や前提、トレードオフを書く。
- 実行時設定値は専用の config モジュール／ファイル（例: `config.py`）に集約し、コードやテスト内のマジックナンバーは避ける。

## Tools & MCP Usage

- ドキュメント参照や前提確認には、context7 (MCP server) を用いて常に最新の docs を取得する。
- ライブラリの API を利用する際は、`resolve-library-id` → `get-library-docs` の順で確認する。
- 複雑なコーディングが必要な場合は codex-cli を用いる（モデル指定はせずデフォルトを使用）。
- ブラウザ起動やブラウザテストが必要な場合は chrome-devtools を用いる。

## Docs & Todo Rules

- 要件定義書は機能ごとにチケットに分割し、`/docs` 配下にマークダウンとして保存する。
- ファイル名は連番付き（例: `001_ui_implementation.md`）とし、チケット単位で管理する。
- 各チケット内でタスクは `- [ ]` で列挙し、完了したら `- [x]` に変更する。

## Conversation Guidelines

- 会話は常に日本語で行う。

## Worklog / 作業管理（必要な場合のみ）

- 日報を運用する場合は、`/daily_report` 配下に `[日報_y_m_d_h_m_v(version)].md` 形式で保存する。
- 作業中断時: `/nakajime` コマンドを使用する。
- 作業終了時: `/shime` コマンドを使用する。

---

# Project-Specific Notes

## Project Context


## このリポジトリの運用ルール
