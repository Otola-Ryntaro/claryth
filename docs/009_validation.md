# 009 検証

## 目的
名前解決、DB判定、OTC配合剤、障害時挙動を自動検証する。

## 対象外
医学的レビューそのものの自動化。

## 依存
003から008。

## タスク
- [x] 名前正規化と誤字候補テストを追加する
- [x] 禁忌、注意、記載なしテストを追加する
- [x] OTC配合剤の最高重大度テストを追加する
- [x] API契約とOrigin制限テストを追加する
- [ ] 薬剤師承認済み全レコードのゴールデンテストを追加する

## 実装状況

昇格時に全対象・全成分の`golden_results.draft.json`を生成し、承認者・日時・承認IDを持つ`golden_results.approved.json`がランタイムDBのSHA-256と全期待値へ100%一致しなければ製品bundleを拒否する検証処理を実装済み。実データの薬剤師承認済みファイルが未作成のため、タスク完了および製品出荷ゲートは未達のままとする。

## 受入条件
コード上の判定ゴールデンケースが100%一致する。

## テスト
`pytest`と拡張機能の型チェック・ブラウザ確認を実行する。
